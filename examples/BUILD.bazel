load("@rules_release_file//:index.bzl", "release")

release(
    name = "release",
    files = [
        "app.json",
    ],
    increments = {
        "foo.bar.Code": "1",
        "foo.baz.Number": "1",
    },
    substitutions = {
        "foo.version": "{BUILD_TIMESTAMP}",
    },
)
